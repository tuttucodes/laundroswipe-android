# Node / tooling
node_modules/
.next/
npm-debug.log*
pnpm-lock.yaml
yarn.lock

# Env & config (never commit real credentials; .env.example is placeholders only)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
config/env.js
config/env*.local.js

# Large signed APK (optional); use NEXT_PUBLIC_PRINT_BRIDGE_APK_URL or force-add to git if small team
public/downloads/*.apk

# Claude Code
.claude/

# Editor / OS
.DS_Store
.vscode/
.idea/

